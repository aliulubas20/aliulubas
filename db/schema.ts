import { sql } from "drizzle-orm";
import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
const created=()=>text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`);
export const mods=sqliteTable("mods",{id:integer("id").primaryKey({autoIncrement:true}),slug:text("slug").notNull().unique(),title:text("title").notNull(),description:text("description").notNull(),createdAt:created(),version:text("version"),imageUrl:text("image_url"),fileUrl:text("file_url")});
export const journals=sqliteTable("journals",{id:integer("id").primaryKey({autoIncrement:true}),slug:text("slug").notNull().unique(),title:text("title").notNull(),description:text("description").notNull(),createdAt:created(),mediaUrl:text("media_url"),mediaType:text("media_type")});
export const servers=sqliteTable("servers",{id:integer("id").primaryKey({autoIncrement:true}),slug:text("slug").notNull().unique(),title:text("title").notNull(),description:text("description").notNull(),createdAt:created(),version:text("version"),edition:text("edition"),address:text("address"),port:text("port"),imageUrl:text("image_url"),gallery:text("gallery"),ownerName:text("owner_name"),contact:text("contact"),paymentNote:text("payment_note"),paymentSender:text("payment_sender"),paymentAmount:integer("payment_amount"),durationDays:integer("duration_days"),privateNote:text("private_note"),status:text("status").notNull().default("pending")});
export const settings=sqliteTable("settings",{key:text("key").primaryKey(),value:text("value").notNull()});
export const analytics=sqliteTable("analytics",{id:integer("id").primaryKey({autoIncrement:true}),day:text("day").notNull(),path:text("path").notNull(),event:text("event").notNull(),device:text("device").notNull(),browser:text("browser").notNull(),count:integer("count").notNull().default(1)},t=>[uniqueIndex("analytics_rollup_unique").on(t.day,t.path,t.event,t.device,t.browser)]);
