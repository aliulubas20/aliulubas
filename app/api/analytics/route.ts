import{env}from"cloudflare:workers";import{NextResponse}from"next/server";
function device(ua:string){return /mobile|android|iphone/i.test(ua)?"Mobil":/tablet|ipad/i.test(ua)?"Tablet":"Bilgisayar"}function browser(ua:string){return /edg/i.test(ua)?"Edge":/firefox/i.test(ua)?"Firefox":/chrome/i.test(ua)?"Chrome":/safari/i.test(ua)?"Safari":"Diğer"}
export async function POST(req:Request){if(!env.DB)return NextResponse.json({ok:false},{status:503});const b=await req.json().catch(()=>({}));const path=String(b.path||"/").slice(0,180),event=["view","download","click"].includes(b.event)?b.event:"view",ua=req.headers.get("user-agent")||"";await env.DB.prepare("INSERT INTO analytics (day,path,event,device,browser,count) VALUES (date('now'),?,?,?,?,1) ON CONFLICT(day,path,event,device,browser) DO UPDATE SET count=count+1").bind(path,event,device(ua),browser(ua)).run();return NextResponse.json({ok:true})}
